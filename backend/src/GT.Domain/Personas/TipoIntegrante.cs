namespace GT.Domain.Personas;

/// <summary>
/// Qué es una persona dentro de G&amp;T Logística. El catálogo es fijo en esta versión (FR-026).
/// </summary>
public enum TipoIntegrante : byte
{
    Chofer = 1,
    Empleado = 2,
}
